# Core API server
fastapi>=0.115.0
uvicorn>=0.30.0
python-dotenv>=1.2.3

# LLM inference (Groq via LiteLLM)
litellm>=1.100.1

# Embeddings: ONNX-based, no torch dependency (~50MB vs ~300MB for sentence-transformers)
fastembed>=0.4.1
langchain-community>=0.3.0

# Vector store & RAG
langchain-core>=1.4.0
langchain-text-splitters>=1.1.2

# Multi-turn memory (Upstash Redis REST)
upstash-redis>=1.8.0

# Evaluation tracking
langsmith>=0.12.4

# Document loading
pypdf>=6.18.0
tiktoken>=0.14.0
